spring.application.name=DocuSenseApi

# ==========================================
# 1. PostgreSQL & JPA DataSource Configuration
# ==========================================
spring.datasource.url=jdbc:postgresql://localhost:5432/docusense
spring.datasource.username=postgres
spring.datasource.password=Tmanna
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ==========================================
# 2. OpenAI Configuration
# ==========================================
# Replace with your actual OpenAI API Key, or load it via environment variable: ${OPENAI_API_KEY}

#spring.ai.openai.api-key=YOUR_OPENAI_API_KEY
#spring.ai.openai.chat.options.model=gpt-4o-mini
#spring.ai.openai.embedding.options.model=text-embedding-3-small

# ==========================================
# 3. Spring AI Vector Store (pgvector) Configuration
# ==========================================
# Automatically construct the vector table schemas on start
#spring.ai.vectorstore.pgvector.initialize-schema=true
# Dimension mapping: 1536 for OpenAI text-embedding-3-small, 768 for Google Gecko/Gemini embeddings.
#spring.ai.vectorstore.pgvector.dimensions=1536
#spring.ai.vectorstore.pgvector.index-type=hnsw
#spring.ai.vectorstore.pgvector.distance-type=cosine

# ==========================================
# 4. Redis Configuration (for Semantic Cache)
# ==========================================
#spring.data.redis.host=localhost
#spring.data.redis.port=6379

